import upload from "../config/multer-config";



export const postProfile = async(req:any, res:any)=>{
    try {
        const {name, speciaclty, exprenience, certfication, professionalBio,email, phoneNumber, clinicAddress, city, state, country, pmdcNumber, password, confirmPassword, profilePicture} = req.body
       
    } catch (error) {
        
    }
}